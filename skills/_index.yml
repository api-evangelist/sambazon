generated: '2026-08-05'
method: searched
source: https://www.sambazon.com/agents.md
description: >-
  SAMBAZON publishes its own agent operating instructions at
  https://www.sambazon.com/agents.md, and advertises that file as the canonical
  agent-facing description of the store — it is the single entry in
  sitemap_agentic_discovery.xml, and robots.txt points at it in a comment. The
  file is saved here verbatim; nothing in this directory was authored by API
  Evangelist. /llms.txt on the same host is a near-identical mirror of the same
  document (saved separately under ../llms/).
skills:
  - file: sambazon-agents.md
    name: Agent Instructions — SAMBAZON
    format: AGENTS.md
    published_at: https://www.sambazon.com/agents.md
    api: mcp/sambazon-mcp.yml
    tools:
      - search_catalog
      - create_cart
      - create_checkout
      - update_checkout
      - complete_checkout
    covers:
      - Recommended agent path (install the Shopify Shop skill at
        https://shop.app/SKILL.md for buy-for-me / personal shopping agents)
      - UCP discovery at /.well-known/ucp and the MCP endpoint at /api/ucp/mcp
      - The six-step agent flow — discover, search, cart, checkout, fulfill,
        complete
      - Supported UCP versions (2026-04-08 latest stable, 2026-01-23)
      - Rules — human approval required before payment, back off on 429, pass
        context.address_country and context.currency
      - Read-only browsing endpoints that need no authentication
      - Store policy URLs
    x-evidence:
      fetched: '2026-08-05'
      url: https://www.sambazon.com/agents.md
      http_status: 200
      content_type: text/markdown; charset=utf-8
      size_bytes: 4330
notes: >-
  These instructions are generated by the Shopify platform on the merchant's
  behalf rather than hand-written by SAMBAZON, and much of the content is
  Shopify-wide (the Shop skill, the UCP protocol description). The
  store-specific parts are real: the store URL, the live UCP/MCP endpoints on
  this origin, the supported UCP versions, and this store's policy URLs.
